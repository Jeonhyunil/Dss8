변수 앞에 var, 끝에 세미콜론
숫자의 type은 int가 아닌 number(float도 마찬가지)

java에서 null을 사용하는 이유

var a = 1; -> 메모리의 ram 영역에 1이 들어가고 주소값이 생성됨, a라는 것이 주소값을 가지고있다.

if 메모리를 없애고 싶을땐 : a = null; a가 가지고 있는 주소값이 사라지면서 연결이 끊어짐 => garbage collector가 연결되지 않은 자리만 차지하는 메모리를 삭제(GC라고 부른다) -> 메모리 효율 up
